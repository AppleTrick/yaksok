# Shared Components (`src/components`)

## Purpose
Reusable, atomic UI components that are business-logic agnostic.

## Guidelines
- Components here must be **pure UI** (Presentational).
- They must **not** depend on specific `features`.
- Should be highly reusable (Atoms/Molecules level).
- Accept data via props and emit events via callbacks.
